<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # ⚙️ Основные правила для SPA / фронтенда
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
